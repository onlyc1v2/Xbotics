[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "motrix-envs"
version = "0.0.2"
description = "Robot simulation environment library based on MotrixSim providing multi-task RL environments."
authors = [{ name = "Motphys", email = "developers@motphys.com" }]
requires-python = "==3.10.*"
readme = "README.md"
license = "Apache-2.0"
dependencies = [
    "motrixsim>=0.4.0",
]

[project.optional-dependencies]
# 复杂地形从 PNG 直接采样高度，与渲染地形一致，避免穿地/闪现
heightmap = ["Pillow>=9.0.0"]
